const prompt = require("prompt-sync")();


let WithNumber = prompt("Enter the with number");
let numberLength = prompt("Enter the legth number: ");
 
    area = numberLength * WithNumber;
    result = area;

    console.log("The area = " + result+ "\n");

